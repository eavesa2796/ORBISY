generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Lead {
  id           String   @id @default(cuid())
  businessName String?
  email        String
  message      String
  createdAt    DateTime @default(now())
}
